/** localStorage key for the last selected organization ID */
export const LAST_ORG_KEY = 'selo-last-organization-id'

/** localStorage key for the last view type (organization vs one-time) */
export const LAST_VIEW_KEY = 'selo-last-view-type'

/** Cookie name for the selected organization (read server-side by resolveOrganizationId) */
export const SELO_ORG_COOKIE = 'selo-org'
