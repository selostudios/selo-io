# Dependencies
node_modules

# Build outputs
.next
out
build
dist

# Generated files
*.lock
package-lock.json

# Coverage
coverage

# Misc
.vercel
.git
.worktrees

# Vercel rewrites vercel.json during its build step; local prettier
# sees a byte-identical file, but Vercel's version differs enough to
# trip `prettier --check` on every deploy. Excluded to unblock the
# build-time test gate.
vercel.json
